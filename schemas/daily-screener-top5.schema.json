{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["categories", "report_markdown"],
  "additionalProperties": false,
  "properties": {
    "categories": {
      "type": "object",
      "required": ["NASDAQ100", "SP500", "KOSPI200", "KOSDAQ150"],
      "additionalProperties": false,
      "properties": {
        "NASDAQ100": { "$ref": "#/$defs/top10" },
        "SP500": { "$ref": "#/$defs/top10" },
        "KOSPI200": { "$ref": "#/$defs/top10" },
        "KOSDAQ150": { "$ref": "#/$defs/top10" }
      }
    },
    "report_markdown": { "type": "string" }
  },
  "$defs": {
    "top10": {
      "type": "array",
      "minItems": 10,
      "maxItems": 10,
      "items": {
        "type": "object",
        "required": ["rank", "ticker", "source", "reason", "confidence", "risk"],
        "additionalProperties": false,
        "properties": {
          "rank": { "type": "integer", "minimum": 1, "maximum": 10 },
          "ticker": { "type": "string", "minLength": 1 },
          "source": {
            "type": "string",
            "enum": ["minervini", "canslim", "leader", "momentum", "qullamaggie", "mixed"]
          },
          "reason": { "type": "string", "minLength": 1 },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "risk": { "type": "string" }
        }
      }
    }
  }
}
